{
  "name": "pwa",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "setup:dev": "yarn install",
    "setup:build": "yarn install --prod",
    "reset:dev": "rm -rf ./node_modules ./.next ./out ./coverage && yarn setup:dev",
    "upgrade:dev": "rm -rf ./yarn.lock && yarn reset:dev",
    "dev": "next dev",
    "build": "next build && next export",
    "start": "next start",
    "test": "jest"
  },
  "dependencies": {
    "@api-platform/admin": "^2.5.3",
    "next": "^10.0.3",
    "react": "^17.0.1",
    "react-dom": "^17.0.1"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^5.10.0",
    "@testing-library/react": "^11.2.7",
    "babel-jest": "^26.1.0",
    "jest": "^26.1.0"
  }
}
